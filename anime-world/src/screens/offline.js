import React from 'react'
import Loader from '../components/loaderoffline'

const Loadingoff = () => {
  return (
    <div>
      Only shown offline (surprise!)
      <Loader></Loader>
    </div>
  )
}

export default Loadingoff
